<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    android:background="@android:color/white">

    <!-- Ảnh đại diện -->
    <ImageView
        android:id="@+id/imgDestination"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:contentDescription="Destination Image"
        android:src="@drawable/a" />

    <!-- Tên địa điểm -->
    <TextView
        android:id="@+id/txtDestinationName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textColor="#000000"
        android:textStyle="bold"
        android:layout_marginTop="8dp"
        android:text="KDL Sinh Thái Vĩnh Long" />

    <!-- Địa chỉ -->
    <TextView
        android:id="@+id/txtDestinationAddress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="#666666"
        android:layout_marginTop="4dp"
        android:text="123 Đường Du Lịch, Vĩnh Long" />

    <!-- Trạng thái hoạt động -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/txtDestinationStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#008000"
            android:layout_marginTop="4dp"
            android:text="🟢 Đang hoạt động"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/btn_favorite"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:padding="8dp"
            android:src="@drawable/ic_favorite"/>
    </LinearLayout>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
    <!-- Đánh giá -->
    <TextView
        android:id="@+id/txtDestinationRating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="#FFA500"
        android:layout_marginTop="4dp"
        android:text="⭐ 4.5/5 (200 đánh giá)" />

        <!-- nút 3 chấm -->
        <ImageView
            android:id="@+id/btn_more"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:layout_marginLeft="190dp"
            android:src="@drawable/ic_more"
            android:visibility="gone" />

    </LinearLayout>
</LinearLayout>